module.exports = {
	name: 'Reaction',
	description: 'Allow the bot to use the entire message if the comment isnt a command',
	reaction(message) {
		// put code here if you want to use a message if it isnt start with prefix
		return;
	},
};